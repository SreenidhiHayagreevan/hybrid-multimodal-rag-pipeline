# .env.example

# --- LLM Provider (e.g., OpenAI) ---
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="your_openai_api_key_here"

# --- Commercial OCR Provider (e.g., Google Cloud) ---
# For now, this is a placeholder. We will set this up later.
GOOGLE_APPLICATION_CREDENTIALS="path/to/your/google-credentials.json"

# --- Database Credentials for Docker (Postgres) ---
# These values are pre-configured for the docker-compose.yml file.
# You only need to invent a strong password for DB_PASSWORD.
DB_HOST="localhost"
DB_PORT="5432"
DB_USER="postgres"
DB_PASSWORD="your_secure_password"
DB_NAME="rag_db"